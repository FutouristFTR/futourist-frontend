// Algolia imports

// Firestore imports
import places from "api/collections/places";
import users from "api/collections/users";

// const algolia = {};

const firestore = { places, users };

export default { firestore };
